[package]
name = "watcher_server"
version = "0.0.1"
edition = "2024"

[[bin]]
name = "watcher_server"
path = "src/main.rs"

[dependencies]
pages = { path = "../../lib/pages" }

tokio = { version = "1.0", features = ["full"] }
axum = { version = "0.8.4" }
tower-http = { version = "0.5", features = ["fs"] }
