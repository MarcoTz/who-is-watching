[package]
name = "watcher_cli"
version = "0.0.1"
edition = "2024"

[[bin]]
name = "catcher_cli"
path = "src/main.rs"

[dependencies]
sqlite = { path = "../sqlite" }
shows = { path = "../shows" }
tokio = { version = "1.0", features = ["full"] }

turso = "0.1"
