[package]
name = "sqlite"
version = "0.0.1"
edition = "2024"

[dependencies]
shows = { path = "../shows" }
watchers = { path = "../watchers" }
turso = "0.1"
tokio = { version = "1.0", features = ["full"] }
